# Shared
requests>=2.31
python-dotenv>=1.0
anthropic>=0.92

# Word .docx export for the summary report (bundles Pandoc binary, ~100MB)
pypandoc-binary>=1.13

# MCP server for Claude Desktop integration (mcp_server.py)
mcp>=1.2

# GUI (cdi_outlook_gui.py)
ttkbootstrap>=1.10

# Build a standalone .exe for co-worker distribution (optional, only needed by builder)
pyinstaller>=6.0; sys_platform == 'win32'

# Azure / Microsoft Graph path (scrape_outlook.py)
msal>=1.28

# Classic Outlook desktop path (scrape_outlook_local.py) — Windows only
pywin32>=306; sys_platform == 'win32'
